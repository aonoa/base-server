{{- define "update_all" -}}
// Code generated by entgo-template. DO NOT EDIT.

package ent

{{- range $node := $.Nodes }}
{{ $receiver := printf "%sUpdateOne" $node.Name }}

// UpdateAll updates all fields of the {{ $node.Name }} entity (excluding create_time and update_time).
func (u *{{ $receiver }}) UpdateAll({{ $node.Name }} *{{ $node.Name }}) *{{ $receiver }} {
    update := u
    {{- $first := true }}
    {{- range $field := $node.Fields }}
        {{- if not (or (eq $field.Name "create_time") (eq $field.Name "update_time")) }}
            {{- if $first }}
    update = update.Set{{ $field.StructField }}({{ $node.Name }}.{{ $field.StructField }})
                {{- $first = false }}
            {{- else }}
    update = update.Set{{ $field.StructField }}({{ $node.Name }}.{{ $field.StructField }})
            {{- end }}
        {{- end }}
    {{- end }}
    return update
}
{{ end -}}
{{- end -}}